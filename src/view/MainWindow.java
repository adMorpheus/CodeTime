package view;

import javax.swing.*;

public class MainWindow extends JFrame {
    public void init() {

        setTitle("CodeTime");
        setSize(400, 400);
        setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
        setVisible(true);
    }
}
